<!DOCTYPE html>
<head>
<!-- jquery -->
<script src="http://cdn.staticfile.org/jquery/3.1.1/jquery.min.js" type="text/javascript"></script>
<!-- bootstrap -->
<link href="http://cdn.staticfile.org/twitter-bootstrap/3.4.1/css/bootstrap.min.css" rel="stylesheet">
<script src="http://cdn.staticfile.org/twitter-bootstrap/3.4.1/js/bootstrap.min.js" type="text/javascript"></script>
<!-- bootstrap-select -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.12.4/css/bootstrap-select.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.12.4/js/bootstrap-select.min.js"></script>
</head>

<div class="contianer text-center">
    <% if flash[:alert] %>
        <div class="alert alert-warning" role="alert">
            <strong><%= flash[:alert] %>!</strong>
            Please input a keyword or choose a category for searching.
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    <% end %>
   
    <style type="text/css">
    body {
    background-image: url("https://en.arij.net/wp-content/uploads/sites/3/2018/07/courseintroimage.jpg");
    background-position: center 0;
    background-repeat: no-repeat;
    background-attachment: fixed;
    background-size: cover;
    -webkit-background-size: cover;
    -o-background-size: cover;
    -moz-background-size: cover;
    -ms-background-size: cover;
  }
    </style>
</div>

<div class="disply-3 font-weight-bold col-lg-4 col-lg-offset-4  \
col-sm-6 col-sm-offset-3 col-xs-8 col-xs-offset-2 container-fluid col-center-block">
    <h1>Welcome to the Training DB Webapp</h1>
    <div style="text-align:center">
    You can browse all courses or make a simple search by inputting a keyword or choosing a category.
    </div>
    <h3 class="center">SEARCH</h3>

    <div style="width:100%; text-align:center; position:relative">
    <div class="row">
        <!--all search -->
        <%=form_tag(all_path, :method => "get") do%>
            <%= button_tag " All Courses", :class => 'btn btn-info glyphicon glyphicon-search col-md-offset-3 col-md-6',:name => nil%>  
        <% end %>
    </div>
    <h1></h1>
    <form role="form">
        <div class="form-group col-md-offset-2 col-md-8"> 
            <!--keyword search -->
            <%= form_tag("/search", :method => :get, class: 'form-inline') do%>
                <div class="input-group ">  
                    <%= search_field_tag :search, params[:search], placeholder: "Keyword", class: "form-control" %>   
                    <span class="input-group-btn">
                        <%= button_tag "", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>
                    </span>  
                </div> 
            <% end %>
        </div>

        <div class="form-group">
            <div class="row-fluid col-md-offset-2 col-md-8">
                <!---category search -->
                <%= form_tag(cate_search_path, :method => "get") do%>
                    <!--name defines params-->
                    <select name ="category_name" class="selectpicker show-tick form-control" title="Choose Category" data-live-search="true">   
                        <% @cates.each do |cate| %>
                            <li>
                                <option ><%= cate.category %> </option>
                            </li>
                        <% end %>
                    </select>
                    <%= button_tag "", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>       
                <% end %>
            </div>
        </div>
    </form>
    </div>

</div>
</div>

</html>
